{
  "name": "static-dom-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "static-dom monorepo: a virtual-DOM-free UI library with precise TypeScript types",
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "test:watch": "pnpm -r test:watch",
    "bench": "pnpm --filter @static-dom/core bench",
    "dev": "pnpm --filter @static-dom/core dev"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  },
  "license": "(Apache-2.0 OR MIT)"
}
